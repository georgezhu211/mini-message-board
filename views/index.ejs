<!DOCTYPE html>
<html>
  <head>
    <title>Index</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <% messages.forEach(message => { %>
    <h1><%= message.user %></h1>
    <p><%= message.text %></p>
    <p><%= message.time %></p>
    <% }); %>
  </body>
</html>
